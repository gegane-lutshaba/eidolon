"""EIDOLON — sovereign delegated agency for faithful digital twins.

The domain-agnostic core: ETHOS (fidelity), THEMIS (authority), KAIROS (action
gate), HORKOS (attestation), BASANOS (certification), plus the SAGE adapter,
capture pipeline, and Domain Profile abstraction.

Two invariants override everything (PRD §0):
1. Default-deny — any authority not explicitly granted is denied.
2. No unattested action — no side effect runs without a HORKOS attestation.
"""

__version__ = "0.1.0"
