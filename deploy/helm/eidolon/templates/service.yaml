apiVersion: v1
kind: Service
metadata:
  name: {{ include "eidolon.fullname" . }}
  labels:
    {{- include "eidolon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "eidolon.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
